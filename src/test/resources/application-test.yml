spring:
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    root: error
    org.springframework: error
    org.hibernate.SQL: off
    org.hibernate.orm.jdbc.bind: off
    com.zaxxer.hikari: off